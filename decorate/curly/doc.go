/*
Package curly defines curly bracket decoration style. The style introduces
those rules.
 - Tags are enclosed in `{` and `}`.
 - Atrributes and modificators are concatenated with `_`.
 - Multiple atrributes are delimited with `+`.

 Examples:
  `{yellow+bg_blue}` - Green text on blue bachground.
  `{bold+bg_intesive_cyan}` - Bold text on high intensive cyan background.
*/
package curly
